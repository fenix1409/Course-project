import React from 'react'
import CoursesFilter from '../FilterCourses'

const Directions = () => {
    return (
        <section className='max-sm:flex-col max-sm:gap-[0px] px-[122px] max-lg:px-[15px] py-[44px] max-lg:py-[50px] max-sm:py-[40px]'>
            <CoursesFilter />
        </section>
    )
}

export default Directions